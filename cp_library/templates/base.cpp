// Khung cơ bản — chép thành main.cpp rồi viết solve().
// Biên dịch:  g++ -std=c++17 -O2 -Wall -Wextra -I <thư mục gốc repo> main.cpp -o sol
// Gộp nộp:    python3 cp_library/tools/bundle.py main.cpp -I . -o submit.cpp
#include <bits/stdc++.h>
using namespace std;
// #include "cp_library/02_data_structures/fenwick.hpp"   // include snippet cần dùng ở đây
// #include "cp_library/templates/debug.hpp"              // thêm -DCP_DEBUG khi biên dịch để bật dbg()

void solve() {
    // TODO: đọc dữ liệu MỘT test case, xử lý, in kết quả.
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();                                                  // đề một test case (mặc định)
    // int t; if (!(cin >> t)) return 0; while (t--) solve();  // đề NHIỀU test case: thay dòng trên bằng dòng này
    return 0;
}
